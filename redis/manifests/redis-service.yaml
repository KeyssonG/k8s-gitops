apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: producao
  labels:
    app: redis
spec:
  selector:
    app: redis
  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379
    nodePort: 31009
  type: LoadBalancer
